import 'package:flutter/material.dart';

class EkuitasPage extends StatelessWidget {
  const EkuitasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pemasukan/Ekuitas'),
      ),
      body: Container(),
    );
  }
}
